<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFF3B0"
    android:padding="16dp">


    <Switch
        android:id="@+id/switchKannada"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ಕನ್ನಡ / English"
        android:textColor="#7F0000"
        android:textStyle="bold"
        android:textSize="18sp"
        android:layout_gravity="end"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp" />

    <TextView
        android:id="@+id/txtSwipeHint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Swipe to see more heroes"
        android:textSize="16sp"
        android:textColor="#7F0000"
        android:gravity="center"
        android:layout_marginBottom="8dp" />

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/heroViewPager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <Button
        android:id="@+id/btnReadAloud"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Read Aloud"
        android:backgroundTint="#FFC107"
        android:textColor="#7F0000" />

    <Button
        android:id="@+id/btnTakeQuiz"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Take Hero Quiz"
        android:backgroundTint="#B71C1C"
        android:textColor="#FFFFFF"
        android:layout_marginTop="10dp" />

</LinearLayout>
